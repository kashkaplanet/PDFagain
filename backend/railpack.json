{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "python": "3.11"
  },
  "installCommand": "npm install && python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt",
  "buildCommand": "npm run build",
  "deploy": {
    "startCommand": ". .venv/bin/activate && npm start"
  }
}
